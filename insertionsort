def insertionSort1(n, arr):
    # Write your code here
    #[2 ,4, 6, 8, 3]
    temp = arr[n-1]
    x = n-1
    while temp < arr[x-1] and x > 0:
        arr[x] = arr[x-1]
        print(*arr)
        x -=1
    arr[x]=temp
    print(*arr)
